namespace WaterSortPuzzle
{
    public enum Color
    {
        /// <summary>
        /// Logically Empty is not a color but it is very comfortable to think of it as a special color.
        /// The code will ensure that a "true" color cannot be added on top of an Empty "color"
        /// </summary>
        Empty,

        DarkBlue,
        DarkBrown,
        ForestGreen,
        Grey,
        Chateau,
        LightBlue,
        LightGreen,
        Pink,
        Purple,
        Red,
        Orange,
        Yellow
    }
}